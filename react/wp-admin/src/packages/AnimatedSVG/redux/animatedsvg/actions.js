import { createAction } from '@reduxjs/toolkit'

export const error = createAction(`ANIMATEDSVG/ERROR`)
export const initted = createAction(`ANIMATEDSVG/INIT`)
export const pathname = createAction(`ANIMATEDSVG/PATHNAME`)
export const scene = createAction(`ANIMATEDSVG/SCENE`)
